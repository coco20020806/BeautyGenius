# picture-makeup

Python implementation of the [`picture-makeup`](../../skills/picture_makeup/) skill: qwen base/enrich prompts + wan2.7-image-pro diagrams on `image_format.png`.

```bash
pip install -e packages/picture-makeup
python scripts/run_picture_makeup.py --parse-run outputs/runs/<timestamp>
```

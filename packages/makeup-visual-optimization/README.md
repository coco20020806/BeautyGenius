# makeup-visual-optimization

Python implementation of the [`makeup-visual-optimization`](../../skills/makeup-visual-optimization/) skill: map questionnaire answers to step patches on `tutorial.json` for practice + picture-makeup.

```bash
pip install -e packages/makeup-visual-optimization
```

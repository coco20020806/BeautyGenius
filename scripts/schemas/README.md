# Schema

权威 JSON Schema 位于：

- `packages/video-parse/video_parse/schemas/beauty_video_analysis.v2.json`（无复刻参考对）
- `packages/video-parse/video_parse/schemas/beauty_video_analysis.v2.1.json`（含 `makeup_replication_refs`）

本目录下的 `beauty_video_analysis.schema.json`（若存在）仅为历史兼容。
